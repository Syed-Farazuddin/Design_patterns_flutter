import 'package:flutter/material.dart';

class DioApiFetching extends StatelessWidget {
  const DioApiFetching({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
